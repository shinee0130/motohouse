-- Зураг бүрийг өнгөтэй холбоно: { "<зургийн url>": "<өнгөний нэр>" }.
-- Хэрэглэгч өнгө сонгоход зөвхөн тухайн өнгөний зургууд харагдана. Өнгөгүй
-- (тэмдэглээгүй) зураг нь бүх өнгөнд харагдана — хэмжээний хүснэгт, савлагаа г.м.
alter table public.gear add column if not exists image_colors jsonb not null default '{}'::jsonb;
